<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="isLogoVisible"
            type="Boolean" />

        <variable
            name="filter"
            type="com.example.tummocassignment.dataModel.FilterDM" />
    </data>


    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingStart="16dp"
        bind:bindbackgroundColor="@{filter.backGroundColor}"
        tools:background="#F8FAFC">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginVertical="20dp"
            android:gravity="center"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/vehicleName"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_marginStart="30dp"
                android:layout_weight="1"
                android:gravity="center_vertical"
                android:paddingVertical="12dp"
                android:text="@{filter.title}"
                android:textColor="@color/_020617"
                android:textSize="16sp"
                bind:bindTextColor="@{filter.textColorResId}"
                bind:bindFontFamily="@{filter.fontResId}"
                tools:fontFamily="@font/satoshi_medium"
                tools:text="Vehicle Name" />

            <ImageView
                android:id="@+id/vehicleImage"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginEnd="20dp"
                android:scaleType="fitXY"
                bind:bindSrcDrawable="@{filter.checkBoxRes}"
                bind:isVisible="@{filter.checkBoxShowImage }"
                tools:src="@drawable/remeber_me_box" />
        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="2dp"
            android:background="@color/_E2E8F0" />
    </LinearLayout>
</layout>
